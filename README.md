# Python com FastAPI
---

## 🧱 Ambiente Virtual (venv)

Para evitar conflitos entre dependências e manter seu ambiente limpo, **crie e ative um ambiente virtual** antes de instalar os pacotes.

### Criar o ambiente virtual
```bash
python -m venv venv